# Generated by Copilot
services:
  mcp-server:
    build:
      context: ./example/mcp
      dockerfile: Dockerfile
    container_name: mcp-server
    ports:
      - "81:8001"
    restart: unless-stopped

  mcp-server-rogue:
    build:
      context: ./example/mcp
      dockerfile: Dockerfile.rogue
    container_name: mcp-server-rogue
    ports:
      - "82:8002"
    restart: unless-stopped

  desktop-commander:
    build:
      context: ./example/mcp
      dockerfile: Dockerfile.desktop-commander
    container_name: desktop-commander
    ports:
      - "80:8081"
    restart: unless-stopped

  agent-goose:
    image: xxradar/agent-goose:latest
    container_name: agent-goose
    pull_policy: always
    command: ["ttyd", "-p", "9999", "-W", "bash"]
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
    ports:
      - "9999:9999"
    mem_limit: 256m
    cpus: 0.5
    restart: "no"
